import React from "react";

const Header = (props) => {
    return (
        <div className="flex flex-row justify-end items-center
        h-full mx-5">
            <div>
                About
            </div>
        </div>
    );
};

export default Header;